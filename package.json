{
  "name": "timeserver",
  "version": "0.0.1",
  "description": "a tiny timeserver gives time via a GET req to /",
  "main": "timeserver.js",
  "dependencies": {
    "restify": "^3.0.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "elsehow",
  "license": "MIT"
}
